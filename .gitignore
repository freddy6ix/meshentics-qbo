# PII / financial-data discipline (charter §6) — never commit raw source data or secrets.
# The repo holds treatments and reasoning only; figures/statements live in the secured store.

# Raw source documents & data exports
*.csv
*.pdf
*.xlsx
*.xls
*.ofx
*.qbo
*.qfx

# Secrets / credentials / tokens
*.env
.env*
!.env.example
!**/.env.example
*.tokens.json
.tokens.json
*secret*
*token*
*credential*
*.key
*.pem
service-account*.json

# Raw card data for the client (PII) — never commit, except the folder's README
client/data/*
!client/data/README.md

# Local/working
.DS_Store
node_modules/
